import { COMMON } from '../../types'
import config from '../../config'



export const startLoading = () => {
  return (dispatch) => {
    
  }
}


export const endLoading = () => {
  return (dispatch) => {
    
  }
}